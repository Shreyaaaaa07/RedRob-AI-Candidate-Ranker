# RedRob AI Candidate Ranker - Backend Foundation

This folder contains a minimal FastAPI service foundation that will later expose the existing AI engine in `src/`.

Currently implemented endpoints:
- `GET /` - service status
- `GET /health` - health check

No AI/business logic is connected yet.

